import { CategoryStringPipe } from './category-string.pipe';

describe('CategoryStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryStringPipe();
    expect(pipe).toBeTruthy();
  });
});
